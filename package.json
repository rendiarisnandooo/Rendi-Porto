{
  "name": "rendi-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website for Rendi Arisnando - MCGG Player",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080 --host",
    "build": "tsc && vite build",
    "preview": "vite preview --port 8080",
    "serve": "vite preview --port 8080"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "gsap": "^3.12.0"
  }
}
